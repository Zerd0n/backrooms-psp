# Optional preprocessing/build overrides. Copy to .env only as a reference;
# export the values you actually need before running a command.
BACKROOMS_ASSET_DIR=source_assets
BACKROOMS_CHASE_WAV=source_assets/GAZ.wav
BACKROOMS_DOCKER_IMAGE=pspdev/pspdev:latest
BACKROOMS_DOCKER_CONFIG=tools/docker-config
